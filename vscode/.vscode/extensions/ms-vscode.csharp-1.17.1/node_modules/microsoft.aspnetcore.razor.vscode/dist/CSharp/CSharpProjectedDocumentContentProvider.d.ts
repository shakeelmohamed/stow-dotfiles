import * as vscode from 'vscode';
import { RazorDocumentManager } from '../RazorDocumentManager';
export declare class CSharpProjectedDocumentContentProvider implements vscode.TextDocumentContentProvider {
    private readonly documentManager;
    static readonly scheme: string;
    private readonly onDidChangeEmitter;
    constructor(documentManager: RazorDocumentManager);
    readonly onDidChange: vscode.Event<vscode.Uri>;
    provideTextDocumentContent(uri: vscode.Uri): Promise<string | undefined>;
    ensureDocumentContent(uri: vscode.Uri): void;
    private documentChanged;
    private findRazorDocument;
}
