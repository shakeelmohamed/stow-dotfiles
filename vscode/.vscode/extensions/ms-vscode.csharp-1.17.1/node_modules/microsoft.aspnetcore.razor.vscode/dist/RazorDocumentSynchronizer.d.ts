import * as vscode from 'vscode';
import { IProjectedDocument } from './IProjectedDocument';
import { RazorDocumentManager } from './RazorDocumentManager';
import { RazorLogger } from './RazorLogger';
export declare class RazorDocumentSynchronizer {
    private readonly documentManager;
    private readonly logger;
    private readonly synchronizations;
    private synchronizationIdentifier;
    constructor(documentManager: RazorDocumentManager, logger: RazorLogger);
    register(): vscode.Disposable;
    trySynchronizeProjectedDocument(hostDocument: vscode.TextDocument, projectedDocument: IProjectedDocument, expectedHostDocumentVersion: number, token: vscode.CancellationToken): Promise<boolean>;
    private removeSynchronization;
    private createSynchronizationContext;
    private textDocumentChanged;
    private documentChanged;
    private getProjectedTextDocumentVersion;
}
