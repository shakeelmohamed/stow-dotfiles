import * as vscode from 'vscode';
import { GenericRequestHandler } from 'vscode-languageclient/lib/main';
import { RazorLanguageServerOptions } from './RazorLanguageServerOptions';
import { RazorLogger } from './RazorLogger';
import { TelemetryReporter } from './TelemetryReporter';
export declare class RazorLanguageServerClient implements vscode.Disposable {
    private readonly telemetryReporter;
    private readonly logger;
    private clientOptions;
    private serverOptions;
    private client;
    private startDisposable;
    private eventBus;
    private isStarted;
    constructor(options: RazorLanguageServerOptions, telemetryReporter: TelemetryReporter, logger: RazorLogger);
    onStart(listener: () => any): vscode.Disposable;
    onStop(listener: () => any): vscode.Disposable;
    start(): Promise<void>;
    sendRequest<TResponseType>(method: string, param: any): Promise<TResponseType>;
    onRequest<TRequest, TReturn>(method: string, handler: GenericRequestHandler<TRequest, TReturn>): Promise<void>;
    dispose(): void;
    private getLogLevelString;
}
