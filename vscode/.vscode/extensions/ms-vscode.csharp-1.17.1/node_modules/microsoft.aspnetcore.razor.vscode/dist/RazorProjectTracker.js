"use strict";
/* --------------------------------------------------------------------------------------------
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License. See License.txt in the project root for license information.
 * ------------------------------------------------------------------------------------------ */
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const RazorProjectChangeKind_1 = require("./RazorProjectChangeKind");
class RazorProjectTracker {
    constructor(razorProjectManager, languageServiceClient) {
        this.razorProjectManager = razorProjectManager;
        this.languageServiceClient = languageServiceClient;
    }
    register() {
        const registration = this.razorProjectManager.onChange(event => this.onProjectChange(event));
        return registration;
    }
    onProjectChange(event) {
        return __awaiter(this, void 0, void 0, function* () {
            if (event.kind === RazorProjectChangeKind_1.RazorProjectChangeKind.added) {
                yield this.languageServiceClient.addProject(event.project.uri);
            }
            else if (event.kind === RazorProjectChangeKind_1.RazorProjectChangeKind.removed) {
                yield this.languageServiceClient.removeProject(event.project.uri);
            }
            else if (event.kind === RazorProjectChangeKind_1.RazorProjectChangeKind.changed) {
                yield this.languageServiceClient.updateProject(event.project);
            }
        });
    }
}
exports.RazorProjectTracker = RazorProjectTracker;
//# sourceMappingURL=RazorProjectTracker.js.map