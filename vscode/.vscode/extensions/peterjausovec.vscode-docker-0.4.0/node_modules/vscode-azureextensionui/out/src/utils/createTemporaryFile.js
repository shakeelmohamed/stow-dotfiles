"use strict";
/*---------------------------------------------------------------------------------------------
 *  Copyright (c) Microsoft Corporation. All rights reserved.
 *  Licensed under the MIT License. See License.txt in the project root for license information.
 *--------------------------------------------------------------------------------------------*/
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const crypto = require("crypto");
const fse = require("fs-extra");
const os = require("os");
const path = require("path");
function createTemporaryFile(fileName) {
    return __awaiter(this, void 0, void 0, function* () {
        const randomFolderNameLength = 12;
        const buffer = crypto.randomBytes(Math.ceil(randomFolderNameLength / 2));
        const folderName = buffer.toString('hex').slice(0, randomFolderNameLength);
        const filePath = path.join(os.tmpdir(), folderName, fileName);
        yield fse.ensureFile(filePath);
        return filePath;
    });
}
exports.createTemporaryFile = createTemporaryFile;
//# sourceMappingURL=createTemporaryFile.js.map